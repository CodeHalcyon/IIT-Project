<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zero Hunger</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #f0f0f0;
            font-family: 'Open Sans', sans-serif;
        }
        .hero-section {
            text-align: center;
            padding: 50px;
            background: linear-gradient(135deg, #6f42c1, #e83e8c);
            color: white;
        }
        .btn-success {
            margin: 10px;
            background-color: #28a745;
            border: none;
        }
        .btn-outline-light {
            margin: 10px;
            border-color: white;
            color: white;
        }
        .quote {
            margin-top: 30px;
            font-style: bold;
            color: #020202;
            text-align: center;
        }
        .container {
            margin-top: 20px;
        }
        .image-section {
            margin: 20px 0;
            text-align: center;
        }
        .image-section img {
            width: 100%;
            max-width: 600px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#"><i class="fas fa-seedling"></i> Zero Hunger</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="fas fa-home"></i> Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register"><i class="fas fa-user-plus"></i> Register</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login"><i class="fas fa-sign-in-alt"></i> Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/donate"><i class="fas fa-hand-holding-heart"></i> Donate</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <section class="hero-section">
        <h1>Help End Hunger</h1>
        <p>Together, we can make a difference by donating or receiving food resources.</p>
        <a href="/donate" class="btn btn-success"><i class="fas fa-hand-holding-heart"></i> Donate Now</a>
        <a href="/receive" class="btn btn-outline-light"><i class="fas fa-hand-receiving"></i> Request Help</a>
    </section>

    <div class="container">
        <div class="image-section">
            <img src="donate 3.jpg" class="max-w-xl rounded-lg" alt="">
        </div>
        <blockquote class="quote">“Hunger is not an issue of charity. It is an issue of justice.” – Jacques Diouf</blockquote>
        <blockquote class="quote">“No one should go hungry in a world of plenty.” – Unknown</blockquote>

        <h2 class="text-center mt-5">Our Donors</h2>
        <div class="row mt-4">
            <% if (donors && donors.length > 0) { %>
                <% donors.forEach(donor => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-body">
                                <h5 class="card-title"><%= donor.fullName %></h5>
                                <p class="card-text"><strong>Email:</strong> <%= donor.email %></p>
                                <p class="card-text"><strong>Phone:</strong> <%= donor.phone %></p>
                                <p class="card-text"><strong>Address:</strong> <%= donor.address %></p>
                                <p class="card-text"><strong>Food Type:</strong> <%= donor.foodType %></p>
                                <p class="card-text"><strong>Quantity:</strong> <%= donor.quantity %> kg</p>
                                <p class="card-text"><strong>Location:</strong> <%= donor.location %></p>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p class="text-center">No donors available at the moment.</p>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
